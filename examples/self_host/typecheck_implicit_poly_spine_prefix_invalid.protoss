(defpoly keepFirstListAfterNat (params A)
  (-> (List A) (-> Nat (List A)))
  (lambda (xs (List A))
    (lambda (n Nat) xs)))

(def badPrefixList (List Nat)
  ((keepFirstListAfterNat (Cons true (Nil Bool))) 0))
